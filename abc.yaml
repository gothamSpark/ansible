- name: hello work
  hosts: localhost
  become: yes
  vars:
    Course: "DevOps-from outside task"
    Trainer: "Sivakumar"
    Duration: "110HRS"
  vars_files:
  - variables.yaml

  tasks:
    - name: check variable
      
      ansible.builtin.debug:
        msg: "hi {{ Course }} {{ Trainer }} {{ Duration }} {{a}}"
